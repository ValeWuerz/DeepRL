{
    "version": "2.0.0",
    "tasks": [
     
        {
            "label": "Build and Run Program",
            "type": "shell",
            "command": "cmake --build . && ./tensor",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
       
    ]
}